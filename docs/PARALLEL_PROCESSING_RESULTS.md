# 병렬 처리 테스트 결과

## 테스트 정보
- **날짜**: 2025-11-06
- **오디오 파일**: `wavs/audio_for_whisper_tariff.wav` (14.75분, 885.47초)
- **세그먼트 수**: 4개
- **오버랩**: 5초

## 결과 비교

| 항목 | 원본 (test_pyannote.py) | 병렬 처리 (test_pyannote_parallel.py) | 차이 |
|------|------------------------|--------------------------------------|------|
| **처리 시간** | 82.85초 | 187.01초 | **+125% 느림** ❌ |
| **처리 속도** | 10.69x | 4.73x | **-56% 저하** ❌ |
| **화자 수** | 6명 | 3명 | **-50% 정확도 저하** ❌ |
| **세그먼트 수** | 56개 | 49개 | **-12% 손실** ❌ |

## 문제점 분석

### 1. 성능 저하 (2.25배 느림)

**원인:**
- Windows에서 멀티프로세싱이 제대로 작동하지 않아 순차 처리로 대체됨
- 각 세그먼트마다 파이프라인을 로드해야 함 (초기화 오버헤드)
  - 세그먼트 1: 파이프라인 로드 + 처리
  - 세그먼트 2: 파이프라인 로드 + 처리
  - 세그먼트 3: 파이프라인 로드 + 처리
  - 세그먼트 4: 파이프라인 로드 + 처리
- 총 4번의 파이프라인 로드 시간이 추가됨

**계산:**
- 원본: 1번 파이프라인 로드 (약 2초) + 처리 (약 81초) = 83초
- 병렬: 4번 파이프라인 로드 (약 8초) + 처리 (약 179초) = 187초
- 파이프라인 로드 오버헤드: 약 6초
- 처리 시간 증가: 약 98초 (각 세그먼트가 독립적으로 처리되면서 오버헤드 발생)

### 2. 정확도 저하 (6명 → 3명 화자)

**원인:**
- pyannote는 전체 오디오 컨텍스트를 사용하여 화자를 분리
- 세그먼트로 나누면 세그먼트 간 화자 정보가 손실됨
- 오버랩(5초)이 충분하지 않음
- 세그먼트 경계에서 화자 변경이 제대로 감지되지 않음

**예시:**
- 원본: SPEAKER_00, SPEAKER_01, SPEAKER_02, SPEAKER_03, SPEAKER_04, SPEAKER_05 (6명)
- 병렬: SPEAKER_00, SPEAKER_01, SPEAKER_02 (3명)
- SPEAKER_03, SPEAKER_04, SPEAKER_05가 다른 화자로 잘못 분류되거나 누락됨

### 3. 세그먼트 수 감소 (56개 → 49개)

**원인:**
- 오버랩 구간에서 중복 제거 과정에서 일부 세그먼트가 손실됨
- 병합 로직이 완벽하지 않음
- 세그먼트 경계에서 화자 정보가 불완전함

## Windows 멀티프로세싱 제약

```
[경고] Windows에서 멀티프로세싱은 제한적일 수 있습니다.
각 프로세스가 GPU를 공유할 수 없을 수 있습니다.
순차 처리로 대체합니다...
```

**문제:**
- Windows의 `multiprocessing`은 `spawn` 방식을 사용
- 각 프로세스가 독립적인 Python 인터프리터를 시작
- GPU 컨텍스트를 공유할 수 없음
- 실제 병렬 처리가 이루어지지 않음

## 결론

### ❌ 병렬 처리는 현재 환경에서 효과가 없음

**이유:**
1. **성능 저하**: 2.25배 느림 (187초 vs 83초)
2. **정확도 저하**: 화자 수 50% 감소 (6명 → 3명)
3. **Windows 제약**: 실제 병렬 처리가 이루어지지 않음
4. **초기화 오버헤드**: 각 세그먼트마다 파이프라인 로드 필요

### ✅ 권장 사항

**현재 최적 설정 유지:**
- Mode 8 (FAST mode): 82.85초, 10.69x 속도
- 배치 사이즈 32
- 정확도 100% (6명 화자 모두 감지)

**대안:**
1. **Windows 하드웨어 가속 GPU 스케줄링**: 시스템 설정 최적화
2. **전력 관리 설정**: 고성능 모드
3. **Linux 환경**: 멀티프로세싱이 더 잘 작동할 수 있음 (하지만 여전히 정확도 문제)

### 향후 개선 가능성

1. **더 큰 오버랩**: 5초 → 10-15초 (정확도 향상, 하지만 더 느림)
2. **스마트 병합**: 더 정교한 병합 알고리즘 (복잡도 증가)
3. **Linux 환경**: 실제 병렬 처리 가능 (하지만 정확도 문제는 동일)

**결론**: 현재 환경에서는 원본 방식(test_pyannote.py)이 성능과 정확도 모두에서 우수합니다.

